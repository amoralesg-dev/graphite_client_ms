spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:10.10.31.105}:${DB_PORT:3306}/${DB_NAME:pagos_db}?allowPublicKeyRetrieval=true&useSSL=false
    username: ${DB_USER:user}
    password: ${DB_PASSWORD:user123}

  jpa:
    show-sql: true
