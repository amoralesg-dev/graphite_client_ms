graphite:
  # url graphite
  api-base-url: https://poc.projectgraphite.com/api/public/api/v1
  
  #  datos de autenticación
  auth-url: https://poc.projectgraphite.com/api/oauth/token
  client-id: MX5402.1d2b900374b1
  client-secret: 3d1dbcfce1dee4fc1a846a7eb953d7fb
  
  # Configuración de la interfaz
  interface-name: QAD
  limit: 100
  
  # Configuración del Scheduler
  sync:
    cron: "0 */30 * * * *"



server:
  port: 8080
  servlet:
    context-path: /graphite-client

spring:
  datasource:
    url: jdbc:mysql://10.10.31.6:3306/pagos_db?allowPublicKeyRetrieval=true&useSSL=false
    username: ${DB_USER:user}
    password: ${DB_PASSWORD:user123}
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
